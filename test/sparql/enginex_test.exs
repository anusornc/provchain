defmodule ProvChain.SPARQL.EnginexTest do
  use ExUnit.Case, async: true

  # TODO: Write tests for the new Enginex module here

  describe "Enginex.query/2" do
    # Example test structure
    # test "new feature test" do
    #   graph = RDF.Graph.new() # Or your test graph setup
    #   query = \"\"\"
    #     SELECT ?s WHERE { ?s ?p ?o . }
    #   \"\"\"
    #   assert {:ok, results} = Enginex.query(query, graph)
    #   assert results.results == []
    # end
  end
end
